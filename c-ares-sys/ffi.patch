--- src/ffi.rs.orig	2017-09-11 00:03:37.565221987 +0100
+++ src/ffi.rs	2017-09-11 00:04:29.155102516 +0100
@@ -1,18 +1,20 @@
 /* automatically generated by rust-bindgen */
+#![allow(non_camel_case_types, non_snake_case)]
+use c_types::fd_set;
+use c_types::hostent;
+use c_types::in_addr;
+use c_types::iovec;
+use c_types::sockaddr;
+use c_types::socklen_t;
+use libc::timeval;
+
+#[cfg(windows)]
+pub type ares_socket_t = ::std::os::windows::io::RawSocket;
+#[cfg(unix)]
+pub type ares_socket_t = ::std::os::unix::io::RawFd;
 
 pub type ares_socklen_t = socklen_t;
 pub type ares_ssize_t = isize;
-pub type in_addr_t = u32;
-#[repr(C)]
-#[derive(Debug, Copy)]
-pub struct in_addr {
-    pub s_addr: in_addr_t,
-}
-impl Clone for in_addr {
-    fn clone(&self) -> Self {
-        *self
-    }
-}
 pub type ares_sock_state_cb = ::std::option::Option<
     unsafe extern "C" fn(
         data: *mut ::std::os::raw::c_void,
@@ -394,7 +396,7 @@
     }
 }
 #[repr(C)]
-#[derive(Debug, Copy)]
+#[derive(Copy)]
 pub struct ares_addrttl {
     pub ipaddr: in_addr,
     pub ttl: ::std::os::raw::c_int,
